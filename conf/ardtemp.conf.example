[ardtemp]
# Identity of the board to display. Must match BOARD_ID in the sketch's
# secrets.h. Each physical board needs its own unique value.
board = r4wifi

# URL of the ardtemp-service (K8s NodePort).
# The indicator polls this for live readings; the board posts readings here.
service_url = http://laminarflow:30700

# Alert trigger: temperature swing (°C) within spike_window_s to fire the alert.
spike_threshold_c = 5.0

# Alert trigger: look-back window in seconds.
spike_window_s = 120

# Seconds between HTTP polling retries after a failed request.
reconnect_delay = 5
